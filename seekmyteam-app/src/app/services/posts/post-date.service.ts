import { Injectable } from '@angular/core';

@Injectable()
export class PostDateService {
    constructor() {
    }

    buildDate(date: Date): string {
        // TODO: Build string here that tells how old 'date' is from now
    
        return '';
    }
}